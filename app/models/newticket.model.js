/*const mongoose = require("mongoose");

const NewTicket = mongoose.model(
  "NewTicket",
  new mongoose.Schema({
    idticket: String,
    ticketdetails: String,
    customername: String,
    date: String,
    priority: String
  })
);

module.exports = NewTicket;
*/